#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    cin.tie(0) -> sync_with_stdio(0);
    const int SZ = 100;

    string a, b;
    cin >> a >> b;
    int _a = a.size(), _b = b.size();
    char a_tg = b[0], b_tg = a[0];
    for (int i = _a; i >= 0; i--) {
        
    }
}